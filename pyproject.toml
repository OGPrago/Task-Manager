[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "task-manager-cli"
version = "1.0.0"
description = "A CLI tool to help keep track of your tasks"
requires-python = ">=3.6"

[project.scripts]
task-manager = "main:main"
